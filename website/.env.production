# Production overrides — Vite loads .env then .env.production for builds
# Custom auth domain activates Cloudflare proxy for same-origin auth (fixes Safari)
VITE_FIREBASE_AUTH_DOMAIN=goodmeasuregiving.org
