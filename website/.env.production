# Production overrides — Vite loads .env then .env.production for builds
# Keep Firebase-hosted auth domain unless custom-domain /__/auth proxy is verified.
VITE_FIREBASE_AUTH_DOMAIN=good-measure-giving.firebaseapp.com
# VITE_FIREBASE_USE_CUSTOM_AUTH_DOMAIN=true
