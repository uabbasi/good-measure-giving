# NOT LOADED BY CODE — retained as pointer. Sources of truth:
#   src/constants.py (rate limits, TTLs, timeouts)
#   src/collectors/*.py (per-source endpoints, fields, retry logic)
# This file was an aspirational config layer that was never wired up.
# Collector behavior changes belong in the collector modules, not here.
# History: `git log -- config/data_sources.yaml` has the original config.
