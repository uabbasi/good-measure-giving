# NOT LOADED BY CODE — retained as pointer. Source of truth:
#   src/llm/llm_client.py (MODEL_REGISTRY / TASK_MODELS / LLMTask)
# This file was an aspirational config layer that was never wired up.
# Model / fallback changes belong in llm_client.py, not here.
# History: `git log -- config/llm_config.yaml` has the original tier config.
# Full deletion deferred to the v5.3.0 config consolidation (B13).
