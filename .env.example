# Good Measure Giving - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# =============================================================================
# DOLTDB (Required for data pipeline)
# =============================================================================
# DoltDB provides Git-like version control for the charity data.
# Install: https://docs.dolthub.com/introduction/installation
# Start: `dolt sql-server` in your database directory

DOLT_HOST=127.0.0.1
DOLT_PORT=3306
DOLT_USER=root
DOLT_PASSWORD=
DOLT_DATABASE=good_measure_giving
DOLT_AUTHOR=pipeline
DOLT_EMAIL=pipeline@goodmeasuregiving.local

# =============================================================================
# SUPABASE (Required for website auth only)
# =============================================================================
# Supabase is used only for user authentication and profiles on the website.
# Get from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api-keys

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx  # Publishable key (for website, respects RLS)

# =============================================================================
# API KEYS
# =============================================================================

# Charity Navigator API
# Get your API key from: https://developer.charitynavigator.org/
CN_API_KEY=your_charity_navigator_api_key_here

# Optional: Legacy REST API credentials (if needed)
# CN_APP_ID=your_app_id_here
# CN_APP_KEY=your_app_key_here

# Feature Flags (optional - defaults in config.py)
# ENABLE_SCHOLAR_REVIEW=false
# ENABLE_ZAKAT_CALCULATOR=true

# Logging (optional)
# LOG_LEVEL=INFO
# LOG_FILE=good_measure_giving.log

# LLM Configuration for Reconciliation (005-data-reconciliation)
# Primary: Gemini 2.5 Flash, Backup: GPT-4o mini
LLM_PROVIDER=gemini  # Options: gemini, openai, anthropic
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here  # Backup for GPT-4o mini
RECONCILIATION_BATCH_SIZE=10  # Number of charities to reconcile in parallel

# Used for: automated quality monitoring, experiment tracking, LLM-as-judge scoring
